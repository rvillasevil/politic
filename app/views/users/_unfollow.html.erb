<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Quitar tu apoyo", class: "btn", style: "border: 1px solid #0084B4; background-color: white;" %>
<% end %>